import { Layout } from "antd";

const { Header } = Layout;

const GlobalHeader = () => {
    return (
        <Header style={{ padding: 0, background: "#fff" }}>

        </Header>
    );
};

export default GlobalHeader;
